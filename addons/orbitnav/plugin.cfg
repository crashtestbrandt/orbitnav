[plugin]

name="OrbitNav"
description="Off-thread voxel navigation for Godot 4: occupancy classification, connected components, line of sight and A* pathfinding over a uniform grid, on a worker pool, in a native Rust GDExtension. Registers the `Nav` autoload while enabled; the backend is reached only through nav.gd (enforced by the nav-check gate)."
author="crashtestbrandt"
version="0.2.0"
script="orbitnav.gd"
